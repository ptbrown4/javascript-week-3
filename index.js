// Install package via npm Install
// Load Package with require
var _ = require('lodash');

// Use package based on var we import with

for(var i= 0; i < 100; i++){
var r = _.random(10);

console.log(r);}